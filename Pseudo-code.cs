total fitness
foreach( candidate structure )
{
	sequence = Find all non-interrupted sequences of base-pairs.
	foreach ( sequence NOT in catalytic core)
	{
		total fitness =  total fitness + melting temperature of sequence
	}
}